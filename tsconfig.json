{
  "compilerOptions": {
    "target": "ES6",
    // 指定 ECMAScript 目标版本
    "module": "esnext",
    // 指定生成的模块系统
    "outDir": "./lib",
    // 指定编译输出目录为 lib 文件夹
    "rootDir": "./src",
    // 指定 TypeScript 文件的根目录，可根据实际情况调整
    "strict": true,
    // 启用所有严格类型检查选项
    "esModuleInterop": true,
    // 支持 CommonJS 和 ES 模块之间的互操作性
    "skipLibCheck": true,
    // 跳过对所有 .d.ts 文件的类型检查
    "forceConsistentCasingInFileNames": true,
    // 确保文件名的大小写一致
    "noImplicitAny": false,
    "declaration": true
  },
  "include": [
    "src/**/*.ts"
  ]
  // 指定需要编译的文件路径
}
